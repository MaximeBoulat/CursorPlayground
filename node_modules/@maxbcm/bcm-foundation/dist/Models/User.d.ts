export declare class User {
    accountId: string;
    loggedIn: boolean;
    jwt: string;
    constructor(session: {
        accountId: string;
        loggedIn: boolean;
        jwt: string;
    });
}

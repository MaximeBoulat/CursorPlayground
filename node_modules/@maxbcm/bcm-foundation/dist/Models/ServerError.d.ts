export declare class ServerError<Subcode> extends Error {
    subcode: Subcode;
    constructor(subcode: Subcode, message: string);
    toJSON(): {
        subcode: Subcode;
        message: string;
    };
}

import { ErrorDialogState } from "./ErrorDialogState";
export declare enum UIStateType {
    LOADING = 0,
    READY = 1,
    ERROR = 2
}
export declare class UIState<T> {
    stateType: UIStateType;
    data?: T;
    errorDialogState?: ErrorDialogState;
    constructor(stateType: UIStateType, data?: T, errorDialogState?: ErrorDialogState);
    static loading(): UIState<undefined>;
    static ready<T>(data: T): UIState<T>;
    static error(errorDialogState: ErrorDialogState): UIState<undefined>;
}

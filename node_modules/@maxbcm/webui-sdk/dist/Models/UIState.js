export var UIStateType;
(function (UIStateType) {
    UIStateType[UIStateType["LOADING"] = 0] = "LOADING";
    UIStateType[UIStateType["READY"] = 1] = "READY";
    UIStateType[UIStateType["ERROR"] = 2] = "ERROR";
})(UIStateType || (UIStateType = {}));
export class UIState {
    stateType;
    data;
    errorDialogState;
    constructor(stateType, data, errorDialogState) {
        this.stateType = stateType;
        this.data = data;
        this.errorDialogState = errorDialogState;
    }
    static loading() {
        return new UIState(UIStateType.LOADING, undefined, undefined);
    }
    static ready(data) {
        return new UIState(UIStateType.READY, data, undefined);
    }
    static error(errorDialogState) {
        return new UIState(UIStateType.ERROR, undefined, errorDialogState);
    }
}

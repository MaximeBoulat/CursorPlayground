import { UserInfo } from './UserInfo';
import { ErrorDialogState } from './ErrorDialogState';
import { UIStateType } from './UIStateType';
export declare class UserInfoState {
    stateType: UIStateType;
    userInfo?: UserInfo;
    errorDialogState?: ErrorDialogState;
    constructor(stateType: UIStateType, userInfo?: UserInfo, errorDialogState?: ErrorDialogState);
    static loading(): UserInfoState;
    static userInfo(userInfo: UserInfo): UserInfoState;
    static error(errorDialogState: ErrorDialogState): UserInfoState;
}

import { ErrorDialogState } from '../../Models/ErrorDialogState';
import { ErrorDialog } from '../ErrorDialog/ErrorDialog';
import { SpinnerOverlay } from '../SpinnerOverlay/SpinnerOverlay';
import { UIStateType } from '../../Models/UIState';
import './GenericModal.css';
export declare class GenericModalState {
    stateType: UIStateType;
    errorDialogState?: ErrorDialogState;
    constructor(stateType: UIStateType, errorDialogState?: ErrorDialogState);
    static idle(): GenericModalState;
    static loading(): GenericModalState;
    static error(errorDialogState: ErrorDialogState): GenericModalState;
}
export declare class GenericModal {
    form: HTMLFormElement;
    overlay: SpinnerOverlay;
    closeButton: HTMLButtonElement;
    errorDialog: ErrorDialog;
    element: HTMLDivElement;
    constructor(embed: HTMLFormElement, formTitle: string);
    updateState(state: GenericModalState): void;
}

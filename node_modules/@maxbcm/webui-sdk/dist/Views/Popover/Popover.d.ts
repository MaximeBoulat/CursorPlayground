import './Popover.css';
export interface PopoverOrientation {
    left: boolean;
    up: boolean;
    verticalOffset: number;
    horizontalOffset: number;
}
export declare class Popover {
    constructor(orientation: PopoverOrientation, embed: HTMLElement, anchor: HTMLElement, container?: HTMLElement);
}

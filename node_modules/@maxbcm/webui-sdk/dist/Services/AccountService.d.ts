import { Account } from '@maxbcm/bcm-foundation';
export declare enum RegistrationError {
    VALIDATION_USERNAME = 0,
    VALIDATION_PASSWORD = 1,
    CONFLICT = 2
}
export declare class AccountService {
    forceProduction: boolean;
    constructor(forceProduction?: boolean);
    requestSessionInfo(): void;
    getUserInfo(account: Account): void;
    endSession(): Promise<Account>;
    register(username: string, password: string): Promise<Account>;
    login(username: string, password: string): Promise<Account>;
}

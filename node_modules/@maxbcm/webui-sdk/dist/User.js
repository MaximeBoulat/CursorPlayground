export class User {
    accountId;
    loggedIn;
    jwt;
    constructor(session) {
        this.accountId = session.accountId;
        this.loggedIn = session.loggedIn;
        this.jwt = session.jwt;
    }
}
